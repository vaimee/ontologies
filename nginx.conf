map $http_accept $agora_document {
    default /agora/agora_ontology.html;
    ~*text/turtle /agora/agora_ontology.ttl;
    ~*application/rdf\+xml /agora/agora_ontology.ttl;
}

map $http_accept $fsm_document {
    default /fsm/fsm.html;
    ~*text/turtle /fsm/fsm.ttl;
}

map $http_accept $jsap_document {
    default /jsap/jsap_ontology.html;
    ~*text/turtle /jsap/jsap_ontology.ttl;
}

map $http_accept $messages_document {
    default /msg/messages_ontology.html;
    ~*text/turtle /msg/messages_ontology.ttl;
}

map $http_accept $users_document {
    default /users/users_ontology.html;
    ~*text/turtle /users/users_ontology.ttl;
}

server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    types {
        text/html html;
        text/turtle ttl;
        image/svg+xml svg;
    }

    if ($http_x_forwarded_proto = "http") {
        return 308 https://$host$request_uri;
    }

    location = /agora {
        try_files $agora_document =404;
    }

    location = /agora/criteria {
        try_files $agora_document =404;
    }

    location ~ ^/agora/[0-9]+\.[0-9]+\.[0-9]+$ {
        try_files $agora_document =404;
    }

    location = /fsm {
        try_files $fsm_document =404;
    }

    location ~ ^/fsm/[0-9]+\.[0-9]+\.[0-9]+$ {
        try_files $fsm_document =404;
    }

    location = /jsap {
        try_files $jsap_document =404;
    }

    location ~ ^/jsap/[0-9]+\.[0-9]+\.[0-9]+$ {
        try_files $jsap_document =404;
    }

    location = /msg {
        try_files $messages_document =404;
    }

    location ~ ^/msg/[0-9]+\.[0-9]+\.[0-9]+$ {
        try_files $messages_document =404;
    }

    location = /users {
        try_files $users_document =404;
    }

    location ~ ^/users/[0-9]+\.[0-9]+\.[0-9]+$ {
        try_files $users_document =404;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
